package components

import "github.com/tigorlazuardi/redmage/app/models/render"

templ Body(c render.Context) {
	<body class="bg-gradient-to-t from-[#fbc2eb] to-[#a6c1ee] min-h-screen flex" hx-ext="response-targets">
		@Navbar(c)
		<main class="flex-1">
			{ children... }
		</main>
	</body>
}
